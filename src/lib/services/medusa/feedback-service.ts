export const fetchFeedbacks = async ({ query, sid = null }: any) => {
  try {
    let res: any = {}

    res = {} // await getAPI(`reviews/top?store=${storeId}`, origin)

    return res || {}
  } catch (e) {
    return {}
  }
}
